# API 서버 주소
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# dev/local에서 JWT 없을 때 X-User-Id 헤더 (production에서는 사용하지 않음)
NEXT_PUBLIC_DEV_USER_ID=2
NEXT_PUBLIC_APP_ENV=development

# 기존 변수와 호환 (rewrite용)
NEXT_PUBLIC_API_URL=http://localhost:8080
